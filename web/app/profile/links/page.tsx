// Placeholder — LinksPanel moves here in Step 4
export default function ProfileLinksPage() {
  return (
    <main className="mx-auto w-full max-w-3xl flex-1 px-4 py-8 sm:px-6">
      <p className="text-sm text-text-muted">Links</p>
      <h1 className="mt-1 font-display text-2xl font-semibold text-text">
        Your links
      </h1>
      <p className="mt-3 text-sm text-text-muted">
        Link management will live on this page next. For now use About — the
        old panel is still there until Step 4.
      </p>
    </main>
  );
}
