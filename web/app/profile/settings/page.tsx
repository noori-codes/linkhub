// Placeholder — publish, password, verify move here in Step 5
export default function ProfileSettingsPage() {
  return (
    <main className="mx-auto w-full max-w-3xl flex-1 px-4 py-8 sm:px-6">
      <p className="text-sm text-text-muted">Settings</p>
      <h1 className="mt-1 font-display text-2xl font-semibold text-text">
        Account settings
      </h1>
      <p className="mt-3 text-sm text-text-muted">
        Publish, password, and email verify will move here in Step 5. They’re
        still on About for now.
      </p>
    </main>
  );
}
