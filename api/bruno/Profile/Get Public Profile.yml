info:
  name: Get Public Profile
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/profiles/u/noori"
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
