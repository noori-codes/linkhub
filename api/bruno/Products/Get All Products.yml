info:
  name: Get All Products
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/products"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Auth required. Returns every product (all profiles), newest first.
  Profile is populated with username / displayName / status.
