info:
  name: Get Themes
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/themes"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Public. Lists appearance presets (Classic, Midnight, …).
  Seeded automatically when the API starts.
