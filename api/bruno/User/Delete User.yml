info:
  name: Delete User
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/users/deleteMe"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
