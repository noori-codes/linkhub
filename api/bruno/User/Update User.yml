info:
  name: Update User
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/users/updateMe"
  body:
    type: json
    data: |-
      {
        "firstName": "Laura",
        "lastName": "Updated"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
