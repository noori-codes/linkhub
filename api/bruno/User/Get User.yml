info:
  name: Get User
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
