info:
  name: Delete Link
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:3000/api/v1/links/LINK_ID_HERE
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
