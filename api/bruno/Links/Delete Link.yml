info:
  name: Delete Link
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/links/{{linkId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
