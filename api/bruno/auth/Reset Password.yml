info:
  name: Reset Password
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/users/resetPassword/PASTE_RESET_TOKEN_HERE"
  body:
    type: json
    data: |-
      {
        "password": "newpassword123",
        "passwordConfirm": "newpassword123"
      }
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
